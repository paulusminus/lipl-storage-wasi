[package]
name = "console-logger"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = { version = "0.4.45", default-features = false, features = ["std"] }
wit-bindgen = { version = "0.58.0", features = ["bitflags"] }
