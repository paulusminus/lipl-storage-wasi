[package]
name = "server"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
wit-bindgen = { version = "0.59.0", features = ["bitflags", "async-spawn"] }
